/**
 * Desktop page frame for Market home (Figma 25473:87711 "Max Width").
 *
 * The list and the token detail page share one frame so the two read as the
 * same page: 1140 centered, with each block carrying its own inset — 20 for
 * the banner, tabs and toolbar, 12 for the table (its cells add the remaining
 * 8, which lands the first column on the same 20 line as everything else).
 */
export const MARKET_HOME_CONTENT_MAX_WIDTH = 1140;
export const MARKET_HOME_CONTENT_PADDING = 20;
export const MARKET_HOME_TABLE_PADDING = 12;

/** Figma 25473:87731: 36 header row, 72 body row. */
export const MARKET_HOME_TABLE_HEADER_HEIGHT = 36;
export const MARKET_HOME_TABLE_ROW_HEIGHT = 72;
